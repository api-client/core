export interface SetupConfig {
  httpPort: number;
  httpsPort: number;
  certificatesPort: number;
  chunkedHttpPort: number;
  chunkedHttpsPort: number;
  proxyHttpPort: number;
  proxyHttpsPort: number;
}
